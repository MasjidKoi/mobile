import { MyContributions } from "@/components/profile/MyContributions";

/** 33 My Questions — the "My contributions" screen with the Questions tab active. */
export default function MyQuestionsScreen() {
  return <MyContributions initialTab="questions" />;
}
