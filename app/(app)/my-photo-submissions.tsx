import { MyContributions } from "@/components/profile/MyContributions";

/** 32 My Photo Submissions — the "My contributions" screen with the Photos tab active. */
export default function MyPhotoSubmissionsScreen() {
  return <MyContributions initialTab="photos" />;
}
